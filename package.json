{
  "name": "ts-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "allure:generate": "npx allure generate ./allure-results --clean",
    "allure:serve": "npx allure serve",
    "test": "npx playwright test",
    "docker-compose": "docker-compose up api-tests --abort-on-container-exit --remove-orphans"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@playwright/test": "^1.45.3",
    "@types/node": "^20.14.12",
    "allure-commandline": "^2.29.0",
    "allure-playwright": "^3.0.0-beta.7"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
